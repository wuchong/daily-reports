name: Fluss GitHub Daily Report

on:
  schedule:
    - cron: '30 1 * * *'  # UTC 01:30 = Beijing 09:30
  workflow_dispatch:
    inputs:
      date:
        description: '报告日期 (YYYY-MM-DD), 留空则为昨天'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  generate-report:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Calculate date
        id: date
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            echo "date=${{ github.event.inputs.date }}" >> $GITHUB_OUTPUT
          else
            echo "date=$(date -d 'yesterday' +%Y-%m-%d)" >> $GITHUB_OUTPUT
          fi
      
      - name: Collect GitHub data
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash scripts/fluss-github/collect_data.sh ${{ steps.date.outputs.date }}
      
      - name: Generate summary with Claude
        env:
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/fluss-github/generate_summary.py
      
      - name: Generate reports
        env:
          GITHUB_PAGES_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
        run: python scripts/fluss-github/generate_report.py
      
      - name: Send to DingTalk
        env:
          DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        run: python scripts/fluss-github/send_dingtalk.py
      
      - name: Deploy to pages branch
        run: |
          # Save fluss-github docs to temp
          cp -r docs/fluss-github /tmp/fluss-github-output
          mkdir -p /tmp/fluss-github-output/assets
          cp assets/fluss-github/style.css /tmp/fluss-github-output/assets/
          
          # Remove existing content
          rm -rf docs/* *.json || true
          
          # Checkout or create pages branch
          git fetch origin pages || true
          if git show-ref --verify --quiet refs/remotes/origin/pages; then
            git checkout pages
          else
            git checkout --orphan pages
            git rm -rf . || true
          fi
          
          # Merge fluss-github content (preserve data-ai)
          rm -rf fluss-github || true
          cp -r /tmp/fluss-github-output fluss-github
          
          # Commit and push
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "chore: add daily report for ${{ steps.date.outputs.date }}"
          git push origin pages
